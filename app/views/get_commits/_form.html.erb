<%= form_with(model: get_commit) do |form| %>
  <% if get_commit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(get_commit.errors.count, "error") %> prohibited this get_commit from being saved:</h2>

      <ul>
        <% get_commit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
